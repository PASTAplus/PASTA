==============
Authentication
==============

The authentication mechanism is based on the HTTP Basic Authentication.

The client sends a HTTP request with the following header:

    Authorization: Basic <base64 encoded username:password>

.. toctree::
    :hidden:
