==============
Authentication
==============

At present, PASTA+ only supports user authentication through an LDAP "bind" call to a registered LDAP directory service. Users who successfully bind to their corresponding LDAP account are permitted to utilize internal services of PASTA+.